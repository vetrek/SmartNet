---
phase: 08-in-code-documentation
plan: 01
type: execute
---

<objective>
Add DocC documentation comments to undocumented public APIs.

Purpose: Improve developer experience with inline documentation that appears in Xcode Quick Help.
Output: DocC comments on all public APIs in ApiClient+Async, DownloadEndpoint, QueryParameters, and Response.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference for documentation style (already documented):
@Source/Core/ApiClient+Combine.swift

# Files to document:
@Source/Core/ApiClient+Async.swift
@Source/Endpoint/DownloadEndpoint.swift
@Source/Endpoint/QueryParameters.swift
@Source/Response/Response.swift

**Established patterns:**
- DocC style from ApiClient+Combine.swift: `///` with Parameters, Returns, Throws sections
- Keep docs concise - one-line summary + parameter docs where needed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DocC to ApiClient+Async.swift</name>
  <files>Source/Core/ApiClient+Async.swift</files>
  <action>
Add DocC comments to all 4 public request methods, mirroring the style from ApiClient+Combine.swift:
- `request<D, E>(with:decoder:progressHUD:)` - decoded response
- `request<E>(with:progressHUD:)` where Response == Data
- `request<E>(with:progressHUD:)` where Response == String
- `request<E>(with:progressHUD:)` where Response == Void

Each should have:
- One-line summary describing what it does
- Parameters section with endpoint, decoder (if applicable), progressHUD
- Returns description
- Throws NetworkError cases
  </action>
  <verify>swift build succeeds, DocC comments visible in file</verify>
  <done>All 4 async request methods have DocC documentation matching Combine style</done>
</task>

<task type="auto">
  <name>Task 2: Add DocC to DownloadEndpoint, QueryParameters, Response</name>
  <files>Source/Endpoint/DownloadEndpoint.swift, Source/Endpoint/QueryParameters.swift, Source/Response/Response.swift</files>
  <action>
Add DocC comments to:

**DownloadEndpoint.swift:**
- Struct documentation explaining its purpose (download-specific endpoint)
- Init documentation with parameters

**QueryParameters.swift:**
- Struct documentation explaining its purpose (type-safe query params wrapper)
- Both init methods documented

**Response.swift:**
- Struct documentation explaining it wraps request results
- Public properties: result, request, response, statusCode, value, error
- printCurl() method
  </action>
  <verify>swift build succeeds, DocC comments visible in files</verify>
  <done>All three files have comprehensive DocC documentation on public APIs</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] All public APIs in the 4 files have `///` documentation
- [ ] Documentation style matches existing ApiClient+Combine.swift
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No build errors introduced
- DocC comments follow established patterns
</success_criteria>

<output>
After completion, create `.planning/phases/08-in-code-documentation/08-01-SUMMARY.md`
</output>
